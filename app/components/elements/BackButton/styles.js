import { StyleSheet } from 'react-native';

export const BTYPES = {
  HOME : 'home'

}

export default StyleSheet.create({
  container: {
    flex: 1,
    alignItems: 'center',
    justifyContent: 'center'
  }
});
